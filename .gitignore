# GilaElectromagnetics temporary files, e.g. previously computed Fourier information
tmp/
# ignore latex generation files
docs/*.aux
docs/*.bbl
docs/*.blg
docs/*.fdb_latexmk
docs/*.fls
docs/*.log
docs/*.gz
docs/*Notes.bib
# personal task list file
todo.jl

# Documenter
docs/build/
docs/site/

# MacOS
.DS_Store

# Benchmark data
benchmark/*.jld2
benchmark/*.png
benchmark/results/

# Profiling
statprof

# It is not recommended to push the fixed state that is associated with saving a
# Manifest.toml file. In addition, this breaks the docs CI.
Manifest.toml

# Code coverage (run ] test --coverage)
*.jl.cov
*.jl.*.cov

# notes LaTeX byproducts (only the three documents; scratch dirs hold Julia .log/.out run records)
notes/**/*.aux
notes/**/*.toc
notes/**/*.synctex.gz
notes/integrals.log
notes/integrals.out
notes/moments/moments.log
notes/moments/moments.out
notes/farfield/farfield.log
notes/farfield/farfield.out

# rebuildable far-field geometry tables (110 MB, 14-18 s per cell shape)
notes/farfield/shapetab/
# agent working trees: scripts, logs and intermediate caches, not deliverables
notes/farfield/scratch/work/
notes/farfield/scratch/xwork/
